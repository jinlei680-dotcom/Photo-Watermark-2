import AppKit
import CoreGraphics

enum WatermarkRenderer {
    static func render(image: NSImage, spec: WatermarkSpec) -> NSImage {
        // TODO: 使用 CoreGraphics 在位图上绘制文本并返回新图片
        return image
    }
}